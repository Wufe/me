machine:
  timezone:
    Europe/Rome

  node:
    version:
      6.3.1

dependencies:
  pre:
    - which composer
    - which phpunit
    - which npm
    - ls
    - npm install -g typings
    - npm install
    - typings install
    - cd src && composer install --prefer-dist --no-interaction