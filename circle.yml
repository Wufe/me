machine:
  timezone:
    Europe/Rome

  node:
    version:
      6.3.1

dependencies:
  pre:
    - npm install -g typings
    - npm install
    - typings install
    - wget https://getcomposer.org/installer && chmod +x installer && mv installer /usr/bin/composer
    - cd src && composer install --prefer-dist --no-interaction