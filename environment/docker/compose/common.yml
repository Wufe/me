version: '2'
services:
  redis:
    image: redis:3.0
  probe:
    image: kesshin/ansible
    links:
      - db:db
    tty: true
    environment:
      PROBE_HOST: "db"
      PROBE_PORT: "3306"
    volumes:
      - ../configuration/ansible/probe.yml:/ansible/site.yml
